let phrase = "Hello";

if (true) {
  let user = "John";

  function sayHi() {
    alert(`${phrase}, ${user}`);
  }
}

sayHi(); // выведет 'Hello, John', т.к. if выполняется, то функция существует и вне